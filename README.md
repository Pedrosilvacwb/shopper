## **Starting the Project**

Copy the dotenv example file, install the local dependencies and start the project using Docker Compose, run the following command:

```bash
docker compose up --build
```
